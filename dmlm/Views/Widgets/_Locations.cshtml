@model dmlm.Models.PageModel.Page
@{
    var agentModel = new dmlm.Models.AgentModel().GetAgent(Model);
    var agents = agentModel.ToArray();
    
    //System.Web.UI.ScriptManager.RegisterStartupScript(this, this.GetType(), "CallMyFunction", "DrawChart()", true);
}
<script src="~/Scripts/Chart.min.js"></script>
<div class="col-lg-6" style="width: 100%">
    <div>
        <canvas id="myChart" height="400" width="400"></canvas>
    </div>
</div>
<script>
    var ctx = document.getElementById("myChart");
    var myChart = new Chart(ctx, {
        type: 'bar',
        data: {
            //labels: ["Mark Kidwell", "Ben Mcadoo", "Kimate Richards", "Green", "Purple", "Orange"],
            labels: ["Mark Kidwell", "Ben Mcadoo", "Kimate Richards", "Michael Jackson", "Rogers Nelson", "Leia Organa"],
            datasets: [{
                label: '# of Site Visits',
                data: [12, 15, 3, 5, 19, 3]
            }]
        },
        options: {
            scales: {
                yAxes: [{
                    ticks: {
                        beginAtZero: true
                    }
                }]
            }
        }
    });
</script>